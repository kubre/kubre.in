---
export interface Props {
    title: string;
    image: string;
    publishedAt: Date;
    url: string;
}

const formatter = new Intl.DateTimeFormat("en-IN", {
    year: "numeric",
    month: "long",
    day: "numeric",
});
const { title, image, publishedAt, url } = Astro.props;
---

<a href={url}>
    <div
        class="p-4 flex flex-col gap-y-3 transition-colors hover:bg-zinc-900 rounded"
    >
        <img
            class="invert rounded border borer-gray-500 h-64 w-auto object-contain"
            src={"/assets/" + image}
        />
        <h3 class="text-2xl font-semibold">{title}</h3>
        <span class="text-gray-300 text-md"
            >📆 {formatter.format(new Date(publishedAt))}</span
        >
    </div>
</a>
